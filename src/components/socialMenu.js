import * as React from 'react';
import Image from 'react-bootstrap/Image';

const SocialButton = ({link, imgSrc}) => {
	return(
		<Image src={imgSrc} href={link}>

		</Image>
	)
}
const SocialMenu = ({buttons}) => {
	
}